generator client {
  provider = "prisma-client"
  output   = "./generated"
}

generator factory {
  provider           = "prisma-factory"
  prismaClientModule = "./client"
}

datasource db {
  provider = "postgresql"
}

model Account {
  id        String   @id @default(uuid()) @db.Uuid
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  @@map("accounts")
}
